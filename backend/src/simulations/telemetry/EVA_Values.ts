export enum HEART_RATE {
  NOMINAL_MAX = 93,
  NOMINAL_MIN = 85,
  ERROR_MAX = 120,
  ERROR_MIN = 114,
}

export enum SUIT_PRESSURE {
  NOMINAL_MAX = 4.0,
  NOMINAL_MIN = 3.92,
  ERROR_MAX = 2.5,
  ERROR_MIN = 1.75,
}

export enum FAN_TACHOMETER {
  NOMINAL_MAX = 40000.0,
  NOMINAL_MIN = 39900.0,
  ERROR_MAX = 55000.0,
  ERROR_MIN = 45000.0,
}

export enum O2_PRESSURE {
  NOMINAL_MAX = 780,
  NOMINAL_MIN = 778,
}

export enum O2_RATE {
  NOMINAL_MAX = 1.0,
  NOMINAL_MIN = 0.5,
  ERROR_MAX = 0.4,
  ERROR_MIN = 0.3,
}

export enum BATTERY_CAPACITY {
  NOMINAL_MAX = 45,
  NOMINAL_MIN = 60,
  ERROR_MAX = 30,
  ERROR_MIN = 29.4,
}
